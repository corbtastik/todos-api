---
applications:
- name: ((app.name))
  memory: ((app.memory))
  random-route: true
  # routes:
  # - route: ((app.route))
  path: ((app.artifact))
  buildpack: java_buildpack
  env:
    ((env-key-1)): ((env-val-1))